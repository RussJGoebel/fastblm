% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{tune_cv}
\alias{tune_cv}
\title{Tune hyperparameters via k-fold cross validation}
\usage{
tune_cv(
  y,
  A,
  Q_fun,
  R_inv = NULL,
  theta_init = numeric(0),
  lower = rep(-Inf, length(theta_init)),
  upper = rep(Inf, length(theta_init)),
  log_phi_lower = log(0.01),
  log_phi_upper = log(1000),
  k = 5L,
  score = "mse",
  solver = c("cholesky", "pcg", "woodbury"),
  pcg_tol = 1e-06,
  pcg_maxit = NULL,
  Q_inv = NULL,
  constraint_matrix = NULL,
  X_cov = NULL,
  seed = NULL,
  folds = NULL,
  parallel = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{y}{numeric response vector length n}

\item{A}{n x p design matrix}

\item{Q_fun}{function(theta) -> list with Q (required), log_det_Q (optional)}

\item{R_inv}{n x n inverse noise covariance. NULL = identity.}

\item{theta_init}{named numeric vector of initial theta values}

\item{lower}{lower bounds on theta}

\item{upper}{upper bounds on theta}

\item{log_phi_lower}{lower bound for phi search (log scale)}

\item{log_phi_upper}{upper bound for phi search (log scale)}

\item{k}{number of folds. Default 5.}

\item{score}{scoring function. Default "mse". Options: "mse", "mae"}

\item{solver}{solver to use for each fold fit. One of \code{"cholesky"},
\code{"pcg"}, or \code{"woodbury"}. Default \code{"cholesky"}.}

\item{pcg_tol}{PCG tolerance (only used when solver = "pcg")}

\item{pcg_maxit}{max PCG iterations (only used when solver = "pcg")}

\item{Q_inv}{optional p x p prior covariance or function v -> Q^{-1}v.
Required when solver = "woodbury".}

\item{constraint_matrix}{optional constraint matrix C such that C \\%*\\% gamma = 0
is enforced on the posterior. Supply either this or \code{X_cov}, not both.}

\item{X_cov}{optional n x q matrix of covariates used to compute the constraint
matrix per fold as \code{t(X_cov[train,]) \%*\% A_train}. Supply either this
or \code{constraint_matrix}, not both.}

\item{seed}{optional integer seed for fold assignment reproducibility. Ignored
if \code{folds} is supplied.}

\item{folds}{optional integer vector of length n giving pre-specified fold
assignments. If supplied, \code{k} and \code{seed} are ignored.}

\item{parallel}{logical. If TRUE, evaluates CV folds in parallel using
\code{future.apply::future_lapply}. The parallelization backend is
controlled by the user via \code{future::plan()} before calling this
function. Default FALSE.}

\item{verbose}{logical}
}
\value{
object of class fastblm_tuned
}
\description{
Selects theta and phi by minimizing k-fold CV prediction error.
At each candidate (theta, phi), fits on training folds and evaluates
prediction error on held-out fold.
}
